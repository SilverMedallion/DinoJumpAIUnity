using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Wall : MonoBehaviour
{
   //script simply being used instead of a tag. this is done to avoid using strings. as one syntax error with string in the MoveToTargetAgent code will mess
   //everything up
}
